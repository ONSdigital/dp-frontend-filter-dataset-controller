---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: build
  - name: artifacts
  - name: dp-frontend-dataset-controller

outputs:
  - name: deployment

run:
  path: dp-frontend-dataset-controller/ci/scripts/deployment-bundle.sh
