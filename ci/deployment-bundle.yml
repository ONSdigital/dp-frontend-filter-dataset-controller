---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: build
  - name: artifacts
  - name: dp-frontend-filter-dataset-controller

outputs:
  - name: deployment

run:
  path: dp-frontend-filter-dataset-controller/ci/scripts/deployment-bundle.sh
