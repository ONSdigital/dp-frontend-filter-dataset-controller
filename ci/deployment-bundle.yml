---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: build
  - name: artifacts
  - name: dp-frontent-filter-dataset-controller

outputs:
  - name: deployment

run:
  path: dp-frontent-filter-dataset-controller/ci/scripts/deployment-bundle.sh
