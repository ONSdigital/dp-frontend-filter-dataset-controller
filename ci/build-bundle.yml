---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: artifacts
  - name: dp-frontent-filter-dataset-controller

outputs:
  - name: build

run:
  path: dp-frontent-filter-dataset-controller/ci/scripts/build-bundle.sh
