---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: artifacts
  - name: dp-frontend-filter-dataset-controller

outputs:
  - name: build

run:
  path: dp-frontend-filter-dataset-controller/ci/scripts/build-bundle.sh
