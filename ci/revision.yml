---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: bin
  - name: dp-frontent-filter-dataset-controller

outputs:
  - name: artifacts

run:
  path: dp-frontent-filter-dataset-controller/ci/scripts/revision.sh
